---
description: Export the current opencode session to the Obsidian vault
agent: build
---

Call the `export_to_obsidian` tool to export the CURRENT session to the Obsidian vault.

- Do not pass a `sessionId` — let it default to the current session.
- After it runs, report back the exact file path the tool returned (or the error, if it failed).
- Do not do anything else.

$ARGUMENTS
